#include <stdio.h>
#include <stdlib.h>

int main(){
    int testcases;
    scanf("%d", &testcases);

    while(testcases--){
        int N;
        scanf("%d", &N);

        int nums[N];
        for(int i = 0; i < N; i++) scanf("%d", &nums[i]);

        
    }
}