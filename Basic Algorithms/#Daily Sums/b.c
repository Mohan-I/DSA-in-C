#include <stdio.h>
#include <stdlib.h>

struct Node{
    int data;
    struct Node * next;
}

struct Node* Head = NULL;

void insert(int data){
    struct Node * newdata = (struct Node*) malloc();
}

int main(){

}