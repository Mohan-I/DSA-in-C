#include <stdio.h>

int climbStairs(int n) {
    int a = 0;
    if(i != n){
        for(int i = 1; i <=n ; i++){
            n = n - 1;
            a++;
        }
        for(int i = 1){

        }
    }
    
};

int main(){
    int t;
    printf("Enter The Number to Find out its steps: ");
    scanf("%d", &t);
    int r = climbStairs(t);
}